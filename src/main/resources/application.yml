spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booksdb
    username: postgres
    password: narmin3007
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

  jwt:
    secret: mySuperSecretKeyForLibraryManagementSystem123456
    access-expiration: 900000        # 15 dəqiqə
    refresh-expiration: 604800000    # 7 gün

  spring:
    mail:
      host: smtp.gmail.com
      port: 587
      username: your_email@gmail.com
      password: your_app_password
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true