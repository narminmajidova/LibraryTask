spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booksdb
    username: postgres
    password: narmin3007
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080